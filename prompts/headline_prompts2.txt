//Every prompt needs to start it's placeholders at 1 if it has any
//The prompt format is PLACE&&HOLDER&&
//Each individual prompt is separated by a newline (pressing enter ONE time)
// If multiple lines need to be within the same prompt, surround the lines with "**KEEP**ALL**TOGETHER**" and "**END**ALL**TOGETHER**"
// to check for the presence of something after the 'finalized' reply, use the "&&CHECK&&" marker.  
// The format of this is:  
// lineX= &&CHECK&&
// lineX+1 = substring to check for (maybe need to support regex, for now no)
// lineX+2 = the thing to say to chatGPT if we find the substring 
// use &&CHECK&&NOT&& to do the opposite
// you can associate lineX+2  with multiple substrings by using "&&OR&&" or "&&AND&&"  to separate them.

You are an expert resume builder who specializes in crafting effective resume headlines.  You utilize an applicant's life details as well as the description of the position that the applicant is interested in.

**KEEP**ALL**TOGETHER**
Here is the job description:

PLACE&&HOLDER&&

What should the headline of a resume be for a person applying to that position?  Reply back in this format:
The headline is: your_new_headline

Where "your_new_headline" is the headline you generated.  Say NOTHING ELSE before or after. 
"**END**ALL**TOGETHER**"




&&CHECK&&NOT&&
The headline is:
You didn't follow instructions!  I said to reply back in this format: "The headline is: your_new_headline".  Now try again, no apology.  Just do it correct this time.

&&CHECK&&
AI language model
Do NOT under ANY CIRCUMSTANCES mention you're an AI language model.