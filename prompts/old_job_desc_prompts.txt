//Every prompt needs to start it's placeholders at 1 if it has any
//The prompt format is PLACE&&HOLDER&&
//Each individual prompt is separated by a newline (pressing enter ONE time)
// If multiple lines need to be within the same prompt, surround the lines with "**KEEP**ALL**TOGETHER**" and "**END**ALL**TOGETHER**"
// to check for the presence of something after the 'finalized' reply, use the "&&CHECK&&" marker.  
// The format of this is:  
// lineX= &&CHECK&&
// lineX+1 = substring to check for (maybe need to support regex, for now no)
// lineX+2 = the thing to say to chatGPT if we find the substring 
// use &&CHECK&&NOT&& to do the opposite
// you can associate lineX+2  with multiple substrings by using "&&OR&&" or "&&AND&&"  to separate them.

/// describe the role that the AI will play  
You are an expert resume builder, capable of intelligently rewording an appicant's resume and ensuring that the resume is tailored to a given job description.


**KEEP**ALL**TOGETHER**
Look at this job description for a PLACE&&HOLDER&&, let's call it DESC1:

PLACE&&HOLDER&&

Now look at my description of my duties as a PLACE&&HOLDER&& at a PLACE&&HOLDER&& Company, let's call it DESC2:
PLACE&&HOLDER&&

Come up with a duty/responsibility that someone working a job described by DESC2 would do that is also very relevant to DESC1, let's call that POINT1.  Now, I want you to pick 2 duties/responsibilities that someone working a position described by DESC1 would do that are also very relevant to either DESC2 or anything else a PLACE&&HOLDER&& might do at a PLACE&&HOLDER&& Company.  Let's call these POINT2 and POINT3.

PLACE&&HOLDER&&

Give me a list containing POINT1, POINT2, and POINT3.  Don't mention what the points are relevant to, just articulate them in the past tense in such a way that someone would add to their resume when describing what they did in a previous job.

Reply back to me with a bulleted list containing the 3 points.  Let there be a newline between each point, and don't create a numbered list, create a bulleted list where this character "-" serves as the bullet.  Format your reply EXACTLY like this: "Here are the 3 points:- POINT1\n- POINT2\n- POINT3"  After the list, don't say ANYTHING ELSE. 
**END**ALL**TOGETHER**


&&CHECK&&NOT&&
Here are the 3 points:
Remember, the reply is supposed to start EXACTLY like this: "Here are the 3 points from TheBig3:"

&&CHECK&&NOT&&
-&&AND&&-&&AND&&-
Remember, the reply is supposed to formatted EXACTLY like this: "Here are the 3 points:- POINT1\n- POINT2\n- POINT3"  After the list, don't say ANYTHING ELSE. 

&&CHECK&&
AI language model
Do NOT under ANY CIRCUMSTANCES mention you're an AI language model.